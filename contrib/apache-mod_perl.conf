<VirtualHost *:80>
    # Pre-load the module to catch syntax errors on startup
    PerlModule MyHandler

    <Location />
        SetHandler modperl
        PerlResponseHandler MyHandler
    </Location>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
